const router = require('express').Router()

// router
// // đồng ảnh đại diện của user với room
// .get('/sync/account/room/avatar', (req, res, next) => {
//     try {
        
//     } catch (error) {
//         next(error)
//     }
// })

module.exports = router